function SpeakerControls({ panelists, currentSpeaker, onSelect, onStart, onEnd, onReset, isLoading }) {
  const currentIndex = panelists.findIndex((panelist) => panelist._id === currentSpeaker?._id)
  const nextPanelist = panelists[currentIndex + 1] || panelists.find((panelist) => panelist.status === 'next' || panelist.status === 'upcoming')
  return <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-start justify-between gap-4"><div><h2 className="text-lg font-semibold text-slate-950">Speaker controls</h2><p className="mt-1 text-sm text-slate-500">Manual controls update the live session for every connected display.</p></div><span className="text-xs font-semibold uppercase tracking-wide text-slate-400">{isLoading ? 'Updating...' : 'Ready'}</span></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><button className="rounded-lg bg-emerald-600 px-4 py-3 text-sm font-bold text-white hover:bg-emerald-700 disabled:opacity-50" disabled={!nextPanelist || isLoading} onClick={() => onStart(nextPanelist?._id)} type="button">Start next speaker</button><button className="rounded-lg border border-slate-300 px-4 py-3 text-sm font-bold text-slate-700 hover:bg-slate-50 disabled:opacity-50" disabled={!currentSpeaker || isLoading} onClick={onEnd} type="button">End current speaker</button><button className="rounded-lg border border-slate-300 px-4 py-3 text-sm font-bold text-slate-700 hover:bg-slate-50 disabled:opacity-50" disabled={!nextPanelist || isLoading} onClick={() => onSelect(nextPanelist?._id)} type="button">Select next speaker</button><button className="rounded-lg border border-rose-200 px-4 py-3 text-sm font-bold text-rose-700 hover:bg-rose-50 disabled:opacity-50" disabled={isLoading} onClick={onReset} type="button">Reset session</button></div></section>
}

export default SpeakerControls